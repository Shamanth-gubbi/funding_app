import 'package:flutter/material.dart';
void main(){
  runApp(
   MaterialApp(
     theme: ThemeData(
      textTheme: const TextTheme(
       bodyMedium: TextStyle(fontSize: 40.0,
                             fontFamily: 'Hind'
                            ),
    ).apply(
        bodyColor:Colors.grey[50],
        ),
  ),
      home:Scaffold(
       backgroundColor: Colors.lightBlue,
       body: Center(
         child:Text("Scholarship App"),
         ),
 ),
),
);
  
}
